// import React, {Children} from 'react';
// import ReactDOM from 'react-dom/client';
// import App from './App';
// import state, {onMessageChange, onPostChange} from "./redux/store";
// import {BrowserRouter, Route, Routes} from "react-router-dom";
// import {addPost} from "./redux/store";
// import {addMessage} from "./redux/store";
//
// const root = ReactDOM.createRoot(document.getElementById('root'));
// let rerenderEntireTree = (state) => {
//     root.render(<BrowserRouter> <App state={state} addPost={addPost} addMessage={addMessage}
//                                      onPostChange={onPostChange} onMessageChange={onMessageChange} /> </BrowserRouter>);
// }

